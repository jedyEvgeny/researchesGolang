package main
import "fmt"

func main()  {
  var a int
  fmt.Scan(&a)
  array := make ([] int, a)
  for i:=0; a > i; i++ {
    fmt.Scan(&array[i])
    if i % 2 == 0 {
      fmt.Print(array[i], " ")
    }
  }
}
