package main

import "fmt"

func main() {
  var a, max, summ int
  a = 1
    for a != 0 {
      fmt.Scan(&a)
      if a > max {
        max = a
        summ = 1
      } else if a == max {
        summ ++
      }
    }
  fmt.Println(summ)
}
